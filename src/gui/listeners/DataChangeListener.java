package gui.listeners;

public interface DataChangeListener { //interface para permir que um objeto escute um evento de outro objeto 
	
	void onDataChanged(); //evento para ser disparado quando os dados mudarem
}
